{
  "name": "jafs.github.io",
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "npx http-server -p 8080",
    "build:css": "npx @tailwindcss/cli -i ./css/style.css -o ./css/dist.css --minify",
    "watch:css": "npx @tailwindcss/cli -i ./css/style.css -o ./css/dist.css --watch"
  },
  "dependencies": {
    "gray-matter": "4.0.3",
    "marked": "5.1.1"
  }
}
